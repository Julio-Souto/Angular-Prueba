import { YellowHoverDirective } from './yellow-hover.directive';

describe('YellowHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowHoverDirective();
    expect(directive).toBeTruthy();
  });
});
